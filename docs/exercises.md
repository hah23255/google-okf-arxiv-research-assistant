# Exercises

1. Add 10 new papers and query method-specific terms.
2. Introduce invalid frontmatter and verify validator catches it.
3. Compare `search` sort modes on the same query and explain ranking differences.
4. Run `produce-arxiv-bulk --dry-run` and explain category/recency tradeoffs from the stats.
5. Use `show` to inspect at least three cited docs and verify metadata quality.
6. Add one new docs page and keep link checks passing.
7. Extend tests while preserving legacy `/query` and additive `/search` contracts.
